export class Weather {
    date: Date;
    temp: number;
    uv: number;
    condition: string; // was named "type" first, because I can't find better x) can be cloud, sun, thunder, etc.
    // finally found a better name !
}
